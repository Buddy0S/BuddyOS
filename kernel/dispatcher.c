#include <stdint.h>
#include "uart.h"
#include "proc.h"

void dispatcher(void) {
    // run first process
    while (1) {
        // svc handler gives code

        // call corresponding routine
    }
}
